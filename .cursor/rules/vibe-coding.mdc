---
description: Luna Agent Kit — Vibe coding
alwaysApply: true
---

<!-- luna:generated by scripts/sync-agent-views.mjs — DO NOT EDIT; edit rules/<name>.md -->
# GENERATED — edit the canonical source (rules/), not this file

# Vibe coding

**Accuracy over speed.** Procedures: invoke **`workflow-guide`** then phase skills from `WORKFLOW.md`.

Generic rules (loop, failure modes, implementation, fixing, testing): **`vibe-rules`** skill or plugin `docs/VIBE_RULES.md`. Project-specific rules: repo `RULES.md` if present.

**Closed decisions** → native choice tool (2–4 options; recommended first labeled `(Recommended)`); never free-text-only. Claude Code: `AskUserQuestion` · Cursor: `AskQuestion`. Fallback (tool absent / host forbids, e.g. Cursor Plan mode): numbered A/B/C with the same options.
