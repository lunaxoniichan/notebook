# Decisions (ADRs)

Architecture Decision Records — capture **what** and **why** so agents do not relitigate settled choices.

Use the unified front-matter schema (`type: decision`, `lifecycle: official` while active).
When superseded, set `superseded_by` and move a copy/pointer under `docs/post-official/legacy/`.

Template: `templates/docs/decision.md`
